spec_version: 1

name: data-cleaning-env
type: environment

runtime:
  type: docker

entrypoint:
  command: uvicorn server.app:app --host 0.0.0.0 --port 7860

endpoints:
  reset: /reset
  step: /step
  state: /state